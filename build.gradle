ext.jmeGroup = 'ohyeah'
ext.jmeArchivesBaseName = 'itvgame-client'
ext.jmeVersion = '1.0.0-SNAPSHOT'

configurations {
    refProvide
    compile.extendsFrom(refProvide)
}

repositories {
    maven {
        credentials {
            username 'deployment'
            password '123456'
        }
        url 'http://localhost:8000/nexus/content/groups/public/'
    }
}

apply from: "${System.getenv().IGBT_HOME}/build.gradle"