ext.jmeGroup = 'ohyeah'
ext.jmeArchivesBaseName = 'itvgame-common'
ext.jmeVersion = '1.2.5.0'

configurations {
    refProvide
    compile.extendsFrom(refProvide)
}

repositories {
    maven {
        credentials {
            username 'deployment'
            password '123456'
        }
        url 'http://192.168.16.30:8000/nexus/content/groups/public/'
    }
}

apply from: "${System.getenv().IGBT_HOME}/build.gradle"